# Fuyao Photos icon previews

These six PNGs are appearance previews exported from the current `Sources/App/FuyaoPhoto.icon` design. Xcode compiles the layered `.icon` file as the app icon; the PNGs are not included as separate runtime icons.
