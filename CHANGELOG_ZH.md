# 更新日志

## 27.0 — 2026-09-20

### 修复

- 修复 Android DOM 节点快照导致动态照片 XMP 目录被重复删除的问题，兼容桌面动态节点列表；将 Android DOM 回归检查加入本地与 CI 构建。
- 修复 JPEG 导出末尾绘制清除 HDR 增益图、触发空指针的问题，增加 JPEG 准备及 HDR 单图导出的原生回归用例。

### 照片编辑

- 新增照片内嵌磨砂信息卡，支持 EXIF 参数、署名编辑、短边缩放、文字换行及 80%～180% 独立字号调节。
- 支持原图对照、全屏缩放、SF Mono 加载和自定义字体导入。
- 编辑封面时保留支持的 JPEG Ultra HDR 增益图及动态照片视频、音频；不支持的容器不静默扁平化。

### 信息与设置

- 根据照片 GPS 解析城市和国家，支持重试和手动修改。
- 保存默认摄影者及自定义镜头配置；通过 Camera2 查询可见硬件，按配置的物理／等效焦段与倍率端点匹配。

### 界面

- 采用标准 Material 3 顶栏、字体和输入框形状，保留 Fuyao 动态色与分组。
- 设置、镜头页面和全屏预览接入 Navigation Compose 预测性返回；导出改为 Material 3 底部面板和分段格式选择。
- 完善边到边、键盘避让、自适应检查器布局及大字号显示。
- 新增独立镜头编辑页、就地验证、删除撤销与编辑状态保留。
- 稳定渲染反馈，增加可打断且有边界约束的预览回位及缩放按钮。

### 打包

- 采用营销版本 27.0、1A 构建序列，四变体与五种 ABI 产物在同次构建中共享编号。
- 统一 APK 命名并启用 v1/v2 签名。普通 Release 优先私钥，缺失时使用本机调试签名；Unsigned 变体继续不签名。
