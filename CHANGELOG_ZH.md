# 更新日志

## 1.0.0 — 2026-09-20

### 照片元数据与设置

- 通过系统地名服务将 EXIF GPS 转为城市与国家，支持超时、重试、取消及手动编辑；增加照片元数据授权与原片文件导入。
- 新增设置页，保存默认摄影者、GPS 地点解析开关和未识别机型的 1× 焦距；单图署名不再改变全局默认值。
- 增加 Xiaomi 17 Ultra / Xiaomi 14 镜头档案，包含 `75 MM (3.2X)` 显示；保留已有镜头元数据，未知机型不硬套档案。

### 新增

- Android 单图编辑器：EXIF 参数及手动编辑、照片内嵌磨砂卡片、短边缩放、自动换行、原图对照、缩放预览和原尺寸 JPEG/PNG 导出。
- 独立字号调节，范围 80%～180%，默认保留参考字号 100%。文字始终随照片分辨率缩放，单独加大字号时保持卡片宽度，仅在需要时增加高度。
- 本地 SF Mono Regular 自动加载、自定义字体导入与重置、macOS 字体复制脚本；字体二进制不纳入版本管理。
- 固定分发包校验值的官方 Gradle Wrapper、四构建变体，以及沿用既有 Fuyao 项目结构的双语文档。

### 修复

- 署名增加一行时优先压缩上下留白，保留参考卡片 168 px 高度。
- 构建脚本识别本机 `android-37.0` SDK 目录。
- 移除 API 26 主题中要求 API 27 的导航栏属性，由既有 edge-to-edge 实现在运行时配置外观。
- 使用 PrimaryTabRow 替换已弃用的 Material 3 TabRow。

### 验证

通过 JUnit 执行 68 项核心检查。设备测试已编写并编译，尚未连接设备执行；APK、Lint、签名和构建环境证据见[构建状态](docs/BUILD_STATUS.md)。
